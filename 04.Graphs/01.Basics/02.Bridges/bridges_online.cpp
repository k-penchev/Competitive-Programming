//the graph is dynamic (new edges are being added)
#include <iostream>
#include <algorithm>
#include <vector>

void solve()
{
    
}

void fastIO()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
}

int main()
{
    fastIO();
    solve();
    
    return 0;
}
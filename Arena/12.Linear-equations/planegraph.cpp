#include <iostream>
#include <algorithm>
#include <vector>

void solve()
{
    std::cin >> f >> mod;

    for(int i = 1 ; i <= f ; ++i)
    {
        std::cin >> k;
        
        for(int i = 1 ; i <= k ; ++i)
        {
            
        }
    }
}

void fastIO()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
}

int main()
{
    fastIO();
    solve();
    
    return 0;
}